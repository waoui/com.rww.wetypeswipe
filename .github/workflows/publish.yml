name: 自动同步 WeType-Swipe 正式版

on:
  schedule:
    - cron: "*/15 * * * *"
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - ".github/workflows/publish.yml"

permissions:
  contents: write

concurrency:
  group: sync-wetype-latest-release
  cancel-in-progress: false

jobs:
  sync:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    env:
      SOURCE_REPO: waoui/WeType-Swipe
      GH_TOKEN: ${{ github.token }}

    steps:
      - name: 读取源码仓库最新正式版
        shell: bash
        run: |
          set -euo pipefail

          RELEASE_JSON="$(gh api "repos/${SOURCE_REPO}/releases/latest")"
          SOURCE_TAG="$(jq -r '.tag_name' <<<"${RELEASE_JSON}")"
          IS_DRAFT="$(jq -r '.draft' <<<"${RELEASE_JSON}")"
          IS_PRERELEASE="$(jq -r '.prerelease' <<<"${RELEASE_JSON}")"

          test "${IS_DRAFT}" = "false"
          test "${IS_PRERELEASE}" = "false"
          [[ "${SOURCE_TAG}" =~ ^v([0-9]+\.[0-9]+\.[0-9]+)$ ]]
          VERSION_NAME="${BASH_REMATCH[1]}"

          PROPERTIES="$(curl --fail --location --retry 3 \
            "https://raw.githubusercontent.com/${SOURCE_REPO}/${SOURCE_TAG}/gradle.properties")"
          VERSION_CODE="$(printf '%s\n' "${PROPERTIES}" |
            sed -n 's/^VERSION_CODE=\([0-9][0-9]*\)$/\1/p' |
            head -n 1)"
          DECLARED_NAME="$(printf '%s\n' "${PROPERTIES}" |
            sed -n 's/^VERSION_NAME=\(.*\)$/\1/p' |
            tr -d '\r' |
            head -n 1)"

          test -n "${VERSION_CODE}"
          test "${VERSION_NAME}" = "${DECLARED_NAME}"

          TAG="${VERSION_CODE}-${VERSION_NAME}"
          APK_NAME="WeType_Swipe_LSPosed_v${VERSION_NAME}.apk"
          SUMS_NAME="$(gh api "repos/${SOURCE_REPO}/releases/tags/${SOURCE_TAG}" \
            --jq '.assets[].name | select(endswith("_SHA256SUMS.txt"))' |
            head -n 1)"

          test -n "${SUMS_NAME}"

          {
            echo "SOURCE_TAG=${SOURCE_TAG}"
            echo "VERSION_CODE=${VERSION_CODE}"
            echo "VERSION_NAME=${VERSION_NAME}"
            echo "TAG=${TAG}"
            echo "APK_NAME=${APK_NAME}"
            echo "SUMS_NAME=${SUMS_NAME}"
          } >> "${GITHUB_ENV}"

      - name: 检查是否已经同步
        id: existing
        shell: bash
        run: |
          set -euo pipefail
          if gh release view "${TAG}" >/dev/null 2>&1; then
            echo "exists=true" >> "${GITHUB_OUTPUT}"
            echo "Release ${TAG} 已存在，无需重复发布。"
          else
            echo "exists=false" >> "${GITHUB_OUTPUT}"
          fi

      - name: 下载并校验正式 APK
        if: steps.existing.outputs.exists != 'true'
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p release

          gh release download "${SOURCE_TAG}" \
            --repo "${SOURCE_REPO}" \
            --dir release \
            --pattern "${APK_NAME}"

          gh release download "${SOURCE_TAG}" \
            --repo "${SOURCE_REPO}" \
            --dir release \
            --pattern "${SUMS_NAME}"

          test -s "release/${APK_NAME}"
          test -s "release/${SUMS_NAME}"

          cd release
          EXPECTED="$(awk -v file="${APK_NAME}" '$2 == file { print $1 }' "${SUMS_NAME}")"
          test -n "${EXPECTED}"
          printf '%s  %s\n' "${EXPECTED}" "${APK_NAME}" | sha256sum --check -

      - name: 复制源码 Release 更新说明
        if: steps.existing.outputs.exists != 'true'
        shell: bash
        run: |
          set -euo pipefail
          gh release view "${SOURCE_TAG}" \
            --repo "${SOURCE_REPO}" \
            --json body \
            --jq '.body' > release-notes.md
          test -s release-notes.md

      - name: 发布到 LSPosed 模块仓库
        if: steps.existing.outputs.exists != 'true'
        shell: bash
        run: |
          set -euo pipefail
          gh release create "${TAG}" "release/${APK_NAME}" \
            --target main \
            --title "${VERSION_NAME}" \
            --notes-file release-notes.md \
            --latest

      - name: 刷新现有版本的 Latest 状态
        if: steps.existing.outputs.exists == 'true' && github.event_name == 'push'
        shell: bash
        run: |
          set -euo pipefail
          gh release view "${SOURCE_TAG}" \
            --repo "${SOURCE_REPO}" \
            --json body \
            --jq '.body' > release-notes.md
          test -s release-notes.md
          gh release edit "${TAG}" \
            --title "${VERSION_NAME}" \
            --notes-file release-notes.md \
            --latest
